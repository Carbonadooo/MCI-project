# IMU SSL Module Requirements
# This file contains all packages needed for the imu_ssl module

# Core deep learning framework
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Scientific computing and data processing
numpy>=1.21.0
scipy>=1.8.0
pandas>=1.3.0

# Data handling
h5py>=3.1.0

# Visualization (for debug scripts)
matplotlib>=3.7.0

# Computer vision (already in main requirements)
opencv-python>=4.5.0
Pillow>=8.0.0

# Serial communication (already in main requirements)
pyserial>=3.5

# Note: Standard library modules don't need installation:
# - json, random, os, sys, pathlib (built-in Python modules)

# Installation instructions:
# For conda environment:
# conda install pytorch torchvision torchaudio cpuonly -c pytorch
# conda install scipy
# pip install -r imu_ssl_requirements.txt (for remaining packages)